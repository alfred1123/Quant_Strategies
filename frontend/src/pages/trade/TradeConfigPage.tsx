import { Typography } from '@mui/material';

/** Stub — exchange credential form lands in Phase 1.5. */
export default function TradeConfigPage() {
  return (
    <Typography variant="body1" color="text.secondary">
      Config — broker credentials and account settings (Phase 1.5).
    </Typography>
  );
}
