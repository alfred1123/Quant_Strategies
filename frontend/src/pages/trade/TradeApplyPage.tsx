import { Typography } from '@mui/material';

/** Stub — strategy picker and apply flow land in Phase 1.6 / 1.7. */
export default function TradeApplyPage() {
  return (
    <Typography variant="body1" color="text.secondary">
      Trade — strategy picker, dry-run, and live apply (Phase 1.6 / 1.7).
    </Typography>
  );
}
