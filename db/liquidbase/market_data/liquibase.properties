changelog-file=market_data-changelog.xml
driver=org.postgresql.Driver
liquibase-schema-name=market_data

# Connection values are supplied via LIQUIBASE_COMMAND_* env vars (set in .env).
# Run: source ../../../.env && liquibase --defaults-file=liquibase.properties update
