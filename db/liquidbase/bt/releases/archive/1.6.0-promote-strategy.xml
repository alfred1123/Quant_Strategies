<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- ── DDL: add IS_BEST_IND to BT.STRATEGY + backfill ─────────── -->

    <changeSet id="1.6.0-001-add-is-best-ind" author="alfcheun" context="bt">
        <comment>Add IS_BEST_IND to BT.STRATEGY; backfill Y for active rows (presumed best — no comparison data)</comment>
        <sql splitStatements="true" stripComments="true">
            ALTER TABLE BT.STRATEGY ADD COLUMN IS_BEST_IND CHAR(1);

            UPDATE BT.STRATEGY
               SET IS_BEST_IND = 'N';

            UPDATE BT.STRATEGY
               SET IS_BEST_IND = 'Y'
             WHERE TRANSACT_TO_TS = TIMESTAMPTZ '9999-12-31 00:00:00+00';

            ALTER TABLE BT.STRATEGY ALTER COLUMN IS_BEST_IND SET NOT NULL;
        </sql>
    </changeSet>

    <!-- ── SP: SP_INS_STRATEGY (IS_BEST_IND in INSERT) ────────────── -->

    <changeSet id="1.6.0-002-sp-ins-strategy" author="alfcheun" context="bt" runOnChange="true">
        <comment>SP_INS_STRATEGY: VID 1 gets IS_BEST_IND=Y, VID 2+ gets N</comment>
        <sqlFile path="../../procedures/SP_INS_STRATEGY.sql" relativeToChangelogFile="true"
                 splitStatements="false" stripComments="true"/>
    </changeSet>

    <!-- ── SP: SP_PROMOTE_STRATEGY (new) ──────────────────────────── -->

    <changeSet id="1.6.0-003-sp-promote-strategy" author="alfcheun" context="bt" runOnChange="true">
        <comment>SP_PROMOTE_STRATEGY: flip IS_BEST_IND — exactly one best per STRATEGY_ID</comment>
        <sqlFile path="../../procedures/SP_UPD_PROMOTE_STRATEGY.sql" relativeToChangelogFile="true"
                 splitStatements="false" stripComments="true"/>
    </changeSet>

    <!-- ── SP: SP_GET_STRATEGY (add IS_BEST_IND to cursors) ───────── -->

    <changeSet id="1.6.0-004-sp-get-strategy" author="alfcheun" context="bt" runOnChange="true">
        <comment>SP_GET_STRATEGY: add IS_BEST_IND to cursor SELECT lists; filter on TRANSACT_TO_TS</comment>
        <sqlFile path="../../procedures/SP_GET_STRATEGY.sql" relativeToChangelogFile="true"
                 splitStatements="false" stripComments="true"/>
    </changeSet>

</databaseChangeLog>
