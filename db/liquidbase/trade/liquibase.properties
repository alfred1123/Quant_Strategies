changelog-file=trade-changelog.xml
driver=org.postgresql.Driver
liquibase-schema-name=trade

# Connection values are supplied via LIQUIBASE_COMMAND_URL,
# LIQUIBASE_COMMAND_USERNAME, LIQUIBASE_COMMAND_PASSWORD env vars (set in .env).
# Run: source ../../../.env && liquibase --defaults-file=liquibase.properties update
